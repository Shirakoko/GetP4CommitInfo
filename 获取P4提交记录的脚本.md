# 获取P4提交记录的脚本

## 文件目录说明

项目根目录/
│
├── 脚本.bat                     # 批处理脚本，用于调用 Python 脚本并处理提交信息
├── python_scripts/              # 存放 Python 脚本的文件夹
│   ├── check_date.py            # 检查日期格式和合法性的 Python 脚本
│   └── process_commit_info.py   # 处理提交信息并生成输出文件的 Python 脚本
├── CommitInfo.txt               # 临时文件，保存从 Perforce 提取的原始提交信息
├── jiangruiyuan_commit_info.txt # 示例输出文件，保存处理后的提交信息
└── README.md                    # 项目说明文件

## 使用方法

#### 1. **配置环境**

- 确保已安装 Perforce 命令行工具 (`p4`) 和 Python 3.x。
- 确保已将 `p4` 和 `python` 添加到系统环境变量中。

#### 2. **运行脚本**

- <u>双击GetCommitInfo.bat</u>

#### 3. **脚本执行步骤**

脚本会依次执行以下步骤：

- **检查 Perforce 配置**：

  - 脚本会自动检查当前的 `P4PORT` 和 `P4USER` 配置。

  - 如果 `P4PORT` 不是 `p4.aki.kuro.com:1666`，脚本会提示并自动设置为正确的值。

- **确认用户名**：

  - 脚本会显示当前的 Perforce 用户名（`P4USER`）。

  - 提示用户确认是否获取当前用户的提交记录：
    - 输入 `Y`：继续获取当前用户的提交记录。
    - 输入 `N`：提示用户输入需要获取提交记录的用户名。

- **指定日期范围**：

  - 提示用户是否需要指定日期范围：
    - 输入 `Y`：提示用户输入开始日期和结束日期（格式：`YYYY/MM/DD`）。
    - 输入 `N`：拉取全部提交信息。

- **拉取提交信息**：

  - 脚本会使用 `p4 changes` 命令拉取指定用户的提交信息，并保存到临时文件 `CommitInfo.txt` 中。

- **处理提交信息**：

  - 调用 Python 脚本 `process_commit_info.py`，将提交信息处理并保存到以用户名命名的文件中（例如 `jiangruiyuan_commit_info.txt`）。

- **清理临时文件**：

  - 处理完成后，脚本会删除临时文件 `CommitInfo.txt`。

- **完成**：

  - 脚本执行完毕，提示用户按任意键退出。